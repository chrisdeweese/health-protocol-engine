# Examples

These examples are intentionally small and runnable with `tsx`.

```bash
npm run example:stack
npm run example:rhr
```

They load the bundled JSON library, select protocols, parse a `UserProfile`, call `apply()`, and print a compact summary.
